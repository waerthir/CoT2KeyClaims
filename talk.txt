我们还是继续讨论。我现在大致有点眉目了，如果你认为我的想法没问题，就写进problem2cot的spec里面。1，应该把回答器改成CoT生成器，每次分发之后，对于每个问题产生的第一个CoT，首先要 
  多设置四个标志位，分别是CoT内表示的答案是否和正确答案一致；CoT是否经过gemini3 flash的细节性检查；答案是否和现有解法一样；是否是完整处理好的碎片。使用下面步骤：问题分发给gpt， 
  在CoT生成器生成CoT并保存为method碎片，同时在碎片里面设置好三个标志位；然后接下来，按照我下面描述的顺序，拆分三个器件（也就是三个类）对碎片进行检查，首先先读取生碎片，每个对应 
  的CoT的答案让机器进行完检查之后，都把对应的检查结果写进对应的字段里面并储存，然后下一个机器再读取上一个机器处理好的碎片（这个设计就是为了防止任务跑一半断掉的），检查是否和现  
  有的解法一样（现有的解法检查第四个标志位就可以判断是不是处理好的解法），然后把检查结果写进字段里面并保存，保存为碎片；下一个机器再读取碎片，然后gemini进行检查，得到新碎片后再 
  保存。同时，对于上述步骤，检查答案如果发现答案不一致，将会discard掉；如果答案和现有答案一致，也会discard掉；gemini进行细节性检查会直接输出新的CoT，这一步应该一定会输出一个新  
  的CoT。2，method id改成数字就可以，但是碎片文件的保存和命名还是使用problem id接method id（数字）的形式，命名是为了进行读取的。3，我这里额外多补充以下，多线程是对多问题的多线  
  程，但是每个问题内部就是串行的，所以一个问题的答案使用一个碎片保存就可以。4，再次提醒你，我这个设计是为了应对时刻可能的断网的，因此每次其实都会对碎片进行扫描，然后顺着没有完  
  成的步骤进行。5，关于多解也是，每次进行都会有一个机器检查完整的碎片，如果碎片个数没有和是否多解的指示对上（这也有一个机器指示），那么这次启动进行就会尝试生成一个碎片，但是一  
  个就可以，因为每次都生成一个，最后再收集会更方便。6，每次程序开机，都会检查碎片，如果碎片没有到达完美的状态，就会将碎片继续执行。也就是说，这个分发器会尝试先读取问题，然后看  
  完美碎片，然后把问题和不完美的碎片都分发，因此对于每个分发之后的处理小机器，它们都会附带一个检查功能，如果通过了就会直接传递给下一个机器进行处理。你理解我的要求了吗？设计合理 
  吗？合理就写进spec里面。

  现在我们继续专注于spec。1，cot的设置方面，cot我们现在使用的是文本式的cot，一个cot就是一个字符串，所以不用弄成列表式。2，更改合并逻辑。problem2cot这一层里面，最后生成的结果没  有必要再合并成一个大文件，相对应的，也就不需要设置fragment。因为第一，每个文件都有对应的有无和标志位表示当前的状态，第二，后面还要生成key claims，直接用碎片显然会更加简单，所 以，把碎片全都移到对应的文件夹外面就可以。比如，layercot里面有一个文件夹名字叫aaa存储了aaa对应的项目，那么，aaa里面就是很多碎片文件，每个文件的命名方式和之前的碎片命名方式是  一样的。3，我希望再目前的结构里面，problem2cot的结构里面，设计一个全局的任务管理器，能够读取上一层raw2cot留下来的任务，以及这一层已经有的碎片文件，来进行任务分配。之前我设计  了一个读取器的结构，现在只是更加明显了，负责读取没有完成的任务（只需要知道还没有完成就可以，因为目前的结构设计里面，没有完成的任务导入到工序之后会自动执行没有完成的部分）。4,关于discard的部分，我认为目前是可以直接丢弃的。你认为我的设计合理吗？如果你认为合理，那么请修改补充对应的spec（就是problem2cot那个spec，别的可以暂时不动）。